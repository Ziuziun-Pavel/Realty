import { SelectedOption } from './selectedOption';

export interface DropDownNames  {
  type: Array<SelectedOption>,
  price: Array<SelectedOption>,
  rooms: Array<SelectedOption>,
  region: Array<SelectedOption>,
}
